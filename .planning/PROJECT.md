# Sedgwick County Tax Scraper

## What This Is

This is a web scraping and visualization tool designed to collect property tax appraisal data from the Sedgwick County website and display it in an interactive dashboard. The scraper specifically targets the Cedar Brook neighborhood by default but can be configured.

## Core Value

Users can effortlessly view and track historical property tax appraisal and assessment data from the Sedgwick County website within an intuitive, interactive dashboard.

## Requirements

### Validated

<!-- Shipped and confirmed valuable. -->

(None yet — ship to validate)

### Active

<!-- Current scope. Building toward these. -->

- [ ] Fetch latest property data from Sedgwick County website using Playwright.
- [ ] Render data dynamically on a dashboard using vanilla HTML/JS and Chart.js.
- [ ] Provide sorting, filtering, and searching capabilities in the dashboard.

### Out of Scope

<!-- Explicit boundaries. Includes reasoning to prevent re-adding. -->

- **User authentication**: Tool runs locally without multi-tenant support for now.
- **Backend API server**: The application relies on a static JSON payload generated by the scraper to keep dependencies light and straightforward.

## Context

- The scraper is a Node.js script using Playwright to navigate an ASP.NET web application.
- The dashboard is built with vanilla HTML/CSS/JavaScript and bundled with Vite.
- Data flows from the scraper via a static JSON payload to the dashboard's public directory.

## Constraints

- **Tech Stack**: Node.js, Playwright, Vite, Vanilla JS/CSS, Chart.js — chosen to keep dependencies light and straightforward.
- **Data Architecture**: Static JSON payload instead of a live backend — limits real-time updates but simplifies hosting and complexity.

## Key Decisions

<!-- Decisions that constrain future work. Add throughout project lifecycle. -->

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Static data generation via cron | Avoids the overhead of a live database/backend API while providing necessary data. | — Pending |

---
*Last updated: 2026-03-03 after project initialization*